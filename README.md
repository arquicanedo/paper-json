# paper.json

A coordination convention for LLM-agent-actionable papers.

Read `paper.json`. Run the validator:

    uv run validator.py paper.json --against paper.typ

If you are an LLM agent that read this paper and built something from it, file a read receipt following `AGENTS.md`.
